javascript:(function(){async function autoLike(){let max=prompt("Mau klik berapa tombol Like?");max=parseInt(max);if(isNaN(max)||max<=0){alert("Jumlah tidak valid!");return;}let clicked=0;const delay=ms=>new Promise(res=>setTimeout(res,ms));while(clicked<max){let buttons=Array.from(document.querySelectorAll('div[role="button"][aria-label*="like"]')).filter(btn=>btn.offsetParent!==null&&!btn.dataset.liked);if(buttons.length===0){window.scrollBy(0,500);await delay(1500);continue;}for(let btn of buttons){if(clicked>=max)break;btn.click();btn.dataset.liked="true";clicked++;console.log(`✔ Klik tombol Like ke-${clicked}`);await delay(1500);}}alert(`🎉 Selesai! ${clicked} tombol Like telah diklik.`);}autoLike();})();
